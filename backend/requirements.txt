Django>=5.1,<6.0
wagtail>=6.3,<7.0
django-cors-headers>=4.4,<5.0
python-dotenv>=1.0,<2.0
Pillow>=10.0
psycopg[binary]>=3.1,<4.0  # PostgreSQL driver — only needed when POSTGRES_DB is set
gunicorn>=22.0  # WSGI server for production
