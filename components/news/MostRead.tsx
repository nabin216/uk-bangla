import type { Story } from "@/types";
import { useLanguage } from "@/context/LanguageContext";
export default function MostRead({ stories, onOpen }: { stories: Story[]; onOpen: (story: Story) => void }) { const { language } = useLanguage(); const opinion = stories.slice(0, 3); return <section className="rounded-lg border border-slate-200 bg-white p-5 dark:border-slate-700 dark:bg-slate-800"><div className="mb-3 flex justify-between border-b pb-3"><h2 className="font-serif text-xl font-bold">Opinion &amp; voices</h2><span className="text-[10px] font-bold uppercase text-blue-900">All columns</span></div>{opinion.map((story) => <button key={story.id} onClick={() => onOpen(story)} className="flex w-full items-center gap-3 border-b py-4 text-left last:border-0"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-slate-300 text-[10px]">●</span><span className="min-w-0"><b className="block text-xs text-blue-900 dark:text-amber-400">{story.author}</b><span className="mt-1 block font-serif text-sm font-bold leading-snug">{story.title[language]}</span></span><span className="ml-auto text-base text-amber-500">→</span></button>)}</section>; }
