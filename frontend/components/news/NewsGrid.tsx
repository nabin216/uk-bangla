import type { Story } from "@/types";
import { useLanguage } from "@/context/LanguageContext";
import { useSavedArticles } from "@/context/SavedArticlesContext";
export default function NewsGrid({ stories, onOpen }: { stories: Story[]; onOpen: (story: Story) => void }) { const { language, t } = useLanguage(); const { toggleSavedArticle, savedArticleIds } = useSavedArticles(); return <section className="grid grid-cols-1 gap-5 sm:grid-cols-3">{stories.map((story) =><article key={story.id} className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-800"><img onClick={() => onOpen(story)} src={story.image} alt={story.title.en} className="h-44 w-full cursor-pointer object-cover sm:h-40" /><div className="p-5"><span className="text-[10px] font-bold uppercase text-blue-900">{language === "bn" ? story.categoryBn || story.category : story.category}</span><h3 onClick={() => onOpen(story)} className="mt-2 cursor-pointer font-serif text-xl font-bold leading-tight sm:text-lg">{story.title[language]}</h3><div className="mt-5 flex items-center justify-between gap-2 border-t pt-3 text-sm text-slate-500"><span>{story.readMinutes ?? 4} {t("minRead")}{story.readCount != null ? ` · ${story.readCount.toLocaleString(language === "bn" ? "bn-BD" : "en-GB")} ${t("reads")}` : ""} →</span><button onClick={(e) => { e.stopPropagation(); toggleSavedArticle(story.id); }} className="text-base">{savedArticleIds.includes(story.id) ? "🔖" : "♡"}</button></div></div></article>)}</section>; }
