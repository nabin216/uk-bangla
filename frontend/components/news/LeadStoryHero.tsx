import type { Story } from "@/types";
import { useLanguage } from "@/context/LanguageContext";
import { useSavedArticles } from "@/context/SavedArticlesContext";
export default function LeadStoryHero({ story, onOpen }: { story: Story; onOpen: () => void }) { const { language, t } = useLanguage(); const { savedArticleIds, toggleSavedArticle } = useSavedArticles(); const saved = savedArticleIds.includes(story.id); return <section className="mb-8 grid gap-5 rounded-lg border border-slate-300 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800 sm:grid-cols-2 sm:p-6"><div className="order-2 flex flex-col justify-between p-1 sm:order-1"><div><div className="flex items-center gap-2"><span className="text-[9px] font-bold uppercase text-red-600">{t("lead")}</span><span className="text-[9px] text-slate-400">{story.readMinutes ?? 10} {t("minRead")}{story.readCount != null ? ` · ${story.readCount.toLocaleString(language === "bn" ? "bn-BD" : "en-GB")} ${t("reads")}` : ""}</span></div><h1 onClick={onOpen} className="mt-3 cursor-pointer font-serif text-2xl font-bold leading-[1.05] hover:text-blue-700 dark:hover:text-amber-400 sm:text-4xl">{story.title[language]}</h1><p className="mt-4 max-w-prose text-sm leading-relaxed text-slate-600 dark:text-slate-300">{story.excerpt?.[language] || story.title[language]}</p></div><div className="mt-5 flex justify-between border-t border-slate-200 pt-3"><button onClick={onOpen} className="text-[10px] font-bold uppercase text-blue-900 dark:text-amber-400">{t("readFull")} →</button><button onClick={() => toggleSavedArticle(story.id)} className="text-sm">{saved ? "🔖" : "♡"}</button></div></div><div className="order-1 sm:order-2"><img onClick={onOpen} src={story.image} alt={story.title.en} className="h-52 w-full cursor-pointer rounded object-cover sm:h-72" /></div></section>; }
