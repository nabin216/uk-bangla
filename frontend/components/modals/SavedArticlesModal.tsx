"use client";
import type { Story } from "@/types";
import { useLanguage } from "@/context/LanguageContext";
import { useSavedArticles } from "@/context/SavedArticlesContext";
export default function SavedArticlesModal({ open, onClose, stories, onOpen }: { open: boolean; onClose: () => void; stories: Story[]; onOpen: (story: Story) => void }) { const { language, t } = useLanguage(); const { savedArticleIds } = useSavedArticles(); if (!open) return null; const saved = stories.filter((story) => savedArticleIds.includes(story.id)); return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4" onClick={onClose}><div className="w-full max-w-md rounded-xl bg-white p-6 dark:bg-slate-900" onClick={(e) => e.stopPropagation()}><div className="flex justify-between"><h2 className="font-serif text-xl font-bold">{t("savedArticles")}</h2><button onClick={onClose}>✕</button></div>{saved.length ? saved.map((story) => <button key={story.id} onClick={() => { onOpen(story); onClose(); }} className="mt-3 block w-full rounded border p-3 text-left text-sm">{story.title[language]}</button>) : <p className="py-8 text-center text-sm text-slate-500">{t("noSaved")}</p>}</div></div>; }
