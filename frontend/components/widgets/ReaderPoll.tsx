"use client";
import { useState } from "react";
import { useLanguage } from "@/context/LanguageContext";
export default function ReaderPoll() { const { language, t } = useLanguage(); const [voted, setVoted] = useState(false); return <section className="rounded-lg bg-[#102f57] p-5 text-white"><div className="flex justify-between text-[9px] font-bold uppercase text-amber-400"><span>{t("poll")}</span><span className="text-blue-200">UK VISAS</span></div><p className="my-4 font-serif text-lg font-bold leading-tight">{language === "bn" ? "যুক্তরাজ্যের কি গ্র্যাজুয়েটদের জন্য পোস্ট-স্টাডি ওয়ার্ক ভিসা বৃদ্ধি করা উচিত?" : "Should the UK expand post-study work visas for university graduates?"}</p>{voted ? <p className="text-sm font-bold text-emerald-300">Thank you for voting!</p> : <div className="space-y-2"><button onClick={() => setVoted(true)} className="w-full rounded border border-white/10 bg-white/10 p-3 text-left text-sm">A&nbsp;&nbsp; Yes, expand the route</button><button onClick={() => setVoted(true)} className="w-full rounded border border-white/10 bg-white/10 p-3 text-left text-sm">B&nbsp;&nbsp; Keep the current rules</button></div>}</section>; }
